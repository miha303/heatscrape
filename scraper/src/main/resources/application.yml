spring:
  application:
    name: scraper
  cloud:
    zookeeper:
      connect-string: localhost:2181

scraper:
  delay: 1000
  interval: 60000

management:
  server:
    port: 8080

logging:
  level:
    root: INFO
    org.apache.zookeeper.ClientCnxn: WARN